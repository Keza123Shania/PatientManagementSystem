Patient Management System (a simple java-based system) shows the following OOP principles: 

Encapsulation (private fields), Abstraction (Person class), Inheritance (Doctor/Patient), and Polymorphism (performRole method).
